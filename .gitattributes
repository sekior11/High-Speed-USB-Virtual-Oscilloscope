# ---------------------------------------------------------------------------
# MounRiver Studio 生成目录：obj/ 下是 makefile / *.mk / *.o / *.d / *.elf /
# *.hex / *.map，属于构建产物而非源码。
# 排除出 GitHub 语言统计，避免这些 Makefile 盖过 C 成为仓库主语言。
# 文件本身仍保留在仓库中（含可直接烧录的产物），只是不再参与语言占比计算。
# ---------------------------------------------------------------------------
obj/** linguist-generated linguist-detectable=false
